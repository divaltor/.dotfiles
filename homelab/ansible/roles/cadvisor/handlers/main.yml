---
- name: Reload cAdvisor systemd unit
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart cAdvisor
  ansible.builtin.systemd_service:
    name: cadvisor
    state: restarted
