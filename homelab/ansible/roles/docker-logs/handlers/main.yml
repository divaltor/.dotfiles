---
- name: Reload docker-logs systemd unit
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart docker-logs
  ansible.builtin.systemd_service:
    name: docker-logs
    state: restarted
