# Amp orbs

Connect to the homelab only when the task requires it by running:

```bash
.agents/tailscale-connect
```

Use Tailscale MagicDNS hostnames such as `proxmox` and `homelab`, not the
LAN-only `.local` names.

After finishing your work and before archiving a thread - logout from Tailscale to save ephemeral minutes.
