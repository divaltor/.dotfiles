## Installation

1. Clone the repository `git clone <git@github.com>:Divaltor/.dotfiles.git $HOME/.dotfiles`

1. `stow --adapt .`

Might need to copy `opencode` folder (`tools` especially) manually because it can't handle symlinks yet
