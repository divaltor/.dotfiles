## Core rules

- `websearch` and `webfetch` are default Opencode tools and they are disabled to exclude them from context, but `webfetch` and `websearch` tools in prompt are defined by Parallel MCP. It's different thing
- Same applies for `codesearch`, it's default tool to search code through Exa MCP. We're using Vercel MCP to Grep Github code
